<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <ResourceDictionary.MergedDictionaries>
        <ResourceDictionary Source="../Animations.xaml" />
    </ResourceDictionary.MergedDictionaries>

    <ControlTemplate x:Key="ProgressBarTemplate" TargetType="ProgressBar">
        <Grid Height="6" ClipToBounds="True">

            <Border x:Name="PART_Track"
                    Background="{DynamicResource Neutral4Brush}"
                    CornerRadius="3" />

            <Border x:Name="PART_Indicator"
                    Background="{DynamicResource Primary5Brush}"
                    CornerRadius="3"
                    HorizontalAlignment="Left" />

            <Border x:Name="IndeterminateIndicator"
                    Background="{DynamicResource Primary5Brush}"
                    CornerRadius="3"
                    HorizontalAlignment="Left"
                    Width="120"
                    Visibility="Collapsed">
                <Border.RenderTransform>
                    <TranslateTransform x:Name="IndeterminateTransform" X="-150" />
                </Border.RenderTransform>
            </Border>
        </Grid>

        <ControlTemplate.Triggers>

            <Trigger Property="IsIndeterminate" Value="True">
                <Setter TargetName="PART_Indicator" Property="Visibility" Value="Collapsed" />
                <Setter TargetName="IndeterminateIndicator" Property="Visibility" Value="Visible" />
                <Trigger.EnterActions>
                    <BeginStoryboard x:Name="IndeterminateAnimation">
                        <Storyboard RepeatBehavior="Forever">
                            <DoubleAnimation Storyboard.TargetName="IndeterminateTransform"
                                             Storyboard.TargetProperty="X"
                                             From="-150" To="1500" 
                                             Duration="0:0:1.8">
                                <DoubleAnimation.EasingFunction>
                                    <CubicEase EasingMode="EaseInOut" />
                                </DoubleAnimation.EasingFunction>
                            </DoubleAnimation>
                        </Storyboard>
                    </BeginStoryboard>
                </Trigger.EnterActions>
                <Trigger.ExitActions>
                    <StopStoryboard BeginStoryboardName="IndeterminateAnimation" />
                </Trigger.ExitActions>
            </Trigger>

            <Trigger Property="IsEnabled" Value="False">
                <Setter TargetName="PART_Track" Property="Background" Value="{DynamicResource Neutral3Brush}" />
                <Setter TargetName="PART_Indicator" Property="Background" Value="{DynamicResource Neutral5Brush}" />
                <Setter TargetName="IndeterminateIndicator" Property="Background" Value="{DynamicResource Neutral5Brush}" />
            </Trigger>

            <EventTrigger RoutedEvent="FrameworkElement.Loaded">
                <BeginStoryboard Storyboard="{StaticResource FadeInStoryboard}" />
            </EventTrigger>

        </ControlTemplate.Triggers>
    </ControlTemplate>


    <Style TargetType="ProgressBar">
        <Setter Property="Template" Value="{StaticResource ProgressBarTemplate}" />
        <Setter Property="Minimum" Value="0" />
        <Setter Property="Maximum" Value="100" />
        <Setter Property="Height" Value="6" />
        <Setter Property="BorderThickness" Value="0" />
        <Setter Property="Background" Value="Transparent" />
    </Style>

</ResourceDictionary>